apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: carbonscope
  labels:
    app: backend
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: backend
